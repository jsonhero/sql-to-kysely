import { Token } from './token';
/**
 * 获取 注释 以及 分隔符 等词法信息
 * @param {String} sql
 */
declare function lexer(input: string): Token[];
/**
 * 分割sql
 * @param {String} sql
 */
declare function splitSql(sql: string): any[];
/**
 * 清除注释和前后空格
 * @param {String} sql
 */
declare function cleanSql(sql: string): string;
export { cleanSql, splitSql, lexer };
